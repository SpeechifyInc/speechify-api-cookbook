{
  "name": "tts-typescript-voice-cloning",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Clone a voice from an audio sample, synthesize with it, then delete it (Speechify TTS API).",
  "scripts": {
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@speechify/api": "catalog:",
    "dotenv": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:"
  }
}
