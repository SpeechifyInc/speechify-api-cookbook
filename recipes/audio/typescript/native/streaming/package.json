{
  "name": "tts-typescript-streaming-rest",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Stream synthesized audio to a file with the Speechify TTS REST API directly (no SDK), using fetch.",
  "scripts": {
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "dotenv": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:"
  }
}
