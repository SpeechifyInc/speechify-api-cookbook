[project]
name = "audio-python-native-voice-cloning"
version = "0.1.0"
description = "Clone a voice, synthesize with it, then delete it using the Speechify TTS REST API directly (no SDK)."
requires-python = ">=3.10"
dependencies = [
    "requests>=2.28.0",
    "python-dotenv>=1.0.0",
]
