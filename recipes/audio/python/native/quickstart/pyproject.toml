[project]
name = "audio-python-native-quickstart"
version = "0.1.0"
description = "Synthesize speech to an MP3 file using the Speechify REST API directly (no SDK)."
requires-python = ">=3.10"
dependencies = [
    "requests>=2.28.0",
    "python-dotenv>=1.0.0",
]
