[project]
name = "audio-python-native-streaming"
version = "0.1.0"
description = "Stream synthesized audio to a file using the Speechify TTS REST API directly (no SDK)."
requires-python = ">=3.10"
dependencies = [
    "requests>=2.28.0",
    "python-dotenv>=1.0.0",
]
